import { createContext } from "react";

// cuando llaman la funcion AuthContext esta se encarga de crear el contex y es la encargada de enviar las referencias 
export const AuthContext = createContext();